# RagaNotate — Python Requirements
# github.com/jags111/RagaNotate

# Core (always required)
numpy>=1.24
scipy>=1.10

# MIDI export
midiutil>=1.2

# Audio file I/O
soundfile>=0.12

# AI / ML (optional — needed for ai_encoder, dataset modules)
# transformers>=4.30
# datasets>=2.14
# torch>=2.0

# Dev / testing
pytest>=7.0
ruff>=0.1
mypy>=1.0
